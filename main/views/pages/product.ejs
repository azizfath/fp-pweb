<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
</head>

<body>
    <%- include('../partials/header') %>

        <main>
            <article id="product" class="bg-dark bg-opacity-25 py-5">
                <div class="container">
                    <h2 class="text-center mb-4">Berbagai jaringan internet sesuai kebutuhan</h2>
                    <div id="carousel-product" class="carousel" data-bs-ride="carousel">
                        <div class="carousel-inner px-5" id="product-detail">

                            <% data.forEach((element,index)=>{ %>
                                <div class="<%= index === 0 ? " carousel-item active ": "carousel-item " %>">
                                    <div class="card">
                                        <div class="card-body p-5" style="display: flex; flex-direction: column">
                                            <div class="product-detail">
                                                <div class="product-detail-left">
                                                    <p>Up to</p>
                                                    <p class="fw-bold fs-2">
                                                        <%= element.bandwidth %>
                                                    </p>
                                                    <p>Koneksi dengan
                                                        <%= element.name %>
                                                    </p>
                                                </div>
                                                <div class="product-detail-right">
                                                    <b>Tersedia Layanan</b>
                                                    <div>🌐 Internet</div>
                                                    <div>📞 Telepon</div>
                                                    <div class="fs-4 mt-3">Rp
                                                        <%= element.price %>/bulan</div>
                                                </div>
                                            </div>
                                            <hr>
                                            <p>
                                                <%= element.description %>
                                            </p>
                                            <button type="button" class="btn btn-secondary py-3 px-5 button-center">Pesan Sekarang</button>
                                        </div>
                                    </div>
                                </div>
                                <% }) %>
                        </div>


                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#carousel-product" data-bs-slide="prev">
                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                            <span class="visually-hidden">Previous</span>
                        </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#carousel-product" data-bs-slide="next">
                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                            <span class="visually-hidden">Next</span>
                        </button>
                </div>
                <p class="text-center mt-4">Pilih paket Internet WiFi terbaik untuk kamu. </p>
                </div>
            </article>
        </main>

        <footer>
            <%- include('../partials/footer') %>

        </footer>
</body>

</html>